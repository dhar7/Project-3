This directory will contain the output of the evaluation in the following subdirectories:

    ./tas : Contains the resulting timed automata
    ./traces : Contains the timed log strings
    ./logs : Runtime logs, if enabled